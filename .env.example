# Perfil do Spring (dev ou prod)
SPRING_PROFILES_ACTIVE=dev

# Porta do servidor (opcional, padrão: 8080)
# Heroku define automaticamente via variável $PORT
PORT=8080

# Configuração para Supabase (Produção)
SUPABASE_HOST=db.xxxxxx.supabase.co
SUPABASE_PORT=5432
SUPABASE_DB=postgres
SUPABASE_USER=postgres
SUPABASE_PASSWORD=sua_senha_aqui

# Configuração para PostgreSQL Local (Desenvolvimento)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vzdolci
DB_USER=postgres
DB_PASSWORD=postgres
